# DSM_POWER_ELECTRONICS
Delta-Sigma Modulation (DSM) controls converters like half-bridge and 3-phase inverters using accumulated error instead of duty cycles. It uses high switching (oversampling) to improve accuracy and push noise to high frequencies, reducing low-frequency harmonics. Though losses increase, it gives better tracking and performance.
